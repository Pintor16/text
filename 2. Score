int contador=0;

void setup() {
  size(500,200);
}
void draw() {
  background(0);
  textSize(25);
  fill(0,255,0); 
  text("Score="+contador, 350,25);
}
  
  void mouseClicked() {
  if (contador==contador) {
    contador=contador+1;
  }}
